spring:
  config:
    activate:
      on-profile: dev

  datasource:
    url: jdbc:postgresql://localhost:5432/resenhita
    username: postgres
    password: 12345678
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true


  sql:
    init:
      mode: never

api:
  security:
    token:
      secret: 12345678